import 'package:flutter/material.dart';

class PicaRecommendsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pica推荐'),
      ),
      body: Container(),
    );
  }
}